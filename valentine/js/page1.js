const yesBtn = document.getElementById('yesBtn');
const noBtn = document.getElementById('noBtn');

let noBtnSize = 100; // Начальный размер кнопки "Нет" (%)
let yesBtnSize = 100; // Начальный размер кнопки "Да" (%)
let clickCount = 0; // Счётчик кликов на кнопку "Нет"

// Массив с разными текстами для кнопки "Нет"
const noTexts = [
    "Нет 😢",
    "Ты уверена? 🥺",
    "Подумай ещё разок 💭",
    "Ну пожалуйстаааа 🥺👉👈",
    "Точно нет? 😔",
    "Я буду грустить 😢",
    "Может подумаешь? 🌹",
    "Ты моя жизнь! 💕",
    "Умоляю тебя 😭",
    "Ладно... 😔",
    "🥺🥺🥺",
    "Ты самая красивая! ✨",
    "Я люблю тебя! 💖",
    "Без тебя я не могу! 😢",
    "Ты - моё счастье! 🌈",
    "Последний шанс! ⏰",
    "Ну всё, больше не спрошу... 😔",
    "Ладно, ладно... 🥺"
];

yesBtn.addEventListener('click', () => {
    // Добавляем романтическое сообщение перед переходом
    setTimeout(() => {
        alert('Ура! Я так счастлив! 💕');
        navigateTo('page2.html');
    }, 300);
});

noBtn.addEventListener('click', () => {
    // Меняем текст кнопки
    clickCount++;
    
    if (clickCount < noTexts.length) {
        noBtn.textContent = noTexts[clickCount];
    } else {
        noBtn.textContent = "Ну ладно... 😢";
    }
    
    // Уменьшаем кнопку "Нет" и увеличиваем кнопку "Да"
    noBtnSize -= 10;
    yesBtnSize += 5;
    
    noBtn.style.transform = `scale(${noBtnSize / 100})`;
    yesBtn.style.transform = `scale(${yesBtnSize / 100})`;
    
    // Добавляем позицию, чтобы кнопки не наезжали друг на друга
    if (noBtnSize <= 50) {
        noBtn.style.position = 'absolute';
        noBtn.style.right = '20px';
        noBtn.style.bottom = '20px';
    }
    
    // Когда кнопка "Нет" становится очень маленькой, прячем её
    if (noBtnSize <= 20) {
        noBtn.style.display = 'none';
        setTimeout(() => {
            alert('Ну что ж, Дашулечка, ты всё равно моя валентинка! 💕');
            navigateTo('page2.html');
        }, 500);
    }
    
    // Добавляем плавную анимацию смены текста
    noBtn.style.opacity = '0';
    setTimeout(() => {
        noBtn.style.opacity = '1';
    }, 100);
});

if (clickCount < noTexts.length) {
    noBtn.textContent = noTexts[clickCount];
} else {
    // Случайный выбор после исчерпания списка
    const randomIndex = Math.floor(Math.random() * noTexts.length);
    noBtn.textContent = noTexts[randomIndex];
}